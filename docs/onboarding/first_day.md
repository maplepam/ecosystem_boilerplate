# First day: quick commands

**Single source of truth:** [getting_started.md §2 — Must run these commands](getting_started.md#gs-2) (prerequisites, bootstrap, `generate:miniapps`, `flutter run`, CI parity).

This stub stays so old links keep working; we do **not** duplicate command lists here.
