/// Which typography scale [NorthstarTheme.buildThemeData] applies.
enum NorthstarTypographyStyle {
  /// Material 3 2021 scale tinted with [ColorScheme] (legacy option).
  material3,

  /// V3 NORTHSTAR Figma doc: Lexend Deca (hero / title / page title) +
  /// Inter (headings, labels, body). Uses [google_fonts].
  figmaNorthstarV3,
}
