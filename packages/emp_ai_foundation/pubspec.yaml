name: emp_ai_foundation
description: >
  Cross-app utilities (feature-flag contracts, platform helpers) kept out of
  the design system so DS stays visual-only and lightweight.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.5 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.16.0

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
