name: emp_ai_ecosystem_workspace
description: Workspace root for ecosystem boilerplate packages and apps.
publish_to: none

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  archive: ^4.0.7
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  melos: ^6.3.2
